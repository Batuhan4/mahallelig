// placeholder — overwritten in later batch
import { Text } from "react-native";
import { Screen } from "@/components/Screen";

export default function RewardPlaceholder() {
  return (
    <Screen>
      <Text className="text-ink-900 dark:text-white">Soon</Text>
    </Screen>
  );
}
